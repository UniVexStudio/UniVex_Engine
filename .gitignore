build/
build-*/
*.o
*.obj
compile_commands.json
shader_cache/
*.uveassetdb
*.uvesettings
*.log
imgui.ini
DerivedData/

# .NET build output (Engine/Editor/EditorCoreCs and any future C# project) - dotnet publish's own
# output lands under build/ (already ignored above) via each project's own -o flag, never here.
bin/
obj/
*.dll
*.pdb
